import Foundation

enum AppConstants {
    static let appGroupId = "group.com.tripmeter.TripMeter"
    static let captureURLScheme = "tripmeter"
    static let captureURL = URL(string: "tripmeter://capture")!
    static let captureTripNameUserInfoKey = "tripName"
}
